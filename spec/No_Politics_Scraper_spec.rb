require "spec_helper"

RSpec.describe No_Politics_Scraper do
  it "has a version number" do
    expect(No_Politics_Scraper::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
